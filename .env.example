# Backend URL (required in production — set this in Vercel environment variables)
# Get this from your Render service dashboard after deploying.
VITE_API_URL=https://vin-tracker-api.onrender.com

# Set automatically by Render — do not set manually
# DB_PATH=/data/vins.db
# PORT=3002
